// Placeholder: E2E smoke tests are executed via scripts/playwright_presentation_sequences.js
// This file documents the smoke invocation for CI tracking and local dev.

// Run locally: `npm run test:e2e:noanim`
// The test script performs: Bomb+Breeding -> Representative cards -> long AUTO 50 turns -> per-turn assertions

test.skip('E2E smoke noanim placeholder (run via scripts/playwright_presentation_sequences)', () => {
    // Intentionally skipped; real E2E run is via Playwright script
});